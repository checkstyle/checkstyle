            {
              "id": "${ruleId}",
              "shortDescription": {
                "text": "${shortDescription}"
              },
              "fullDescription": {
                "text": "${fullDescription}"
              }
            }