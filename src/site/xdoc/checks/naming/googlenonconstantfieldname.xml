<?xml version="1.0" encoding="UTF-8"?>
<document xmlns="http://maven.apache.org/XDOC/2.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/XDOC/2.0 https://maven.apache.org/xsd/xdoc-2.0.xsd">
  <head>
    <title>GoogleNonConstantFieldName</title>
  </head>
  <body>
    <section name="GoogleNonConstantFieldName">
      <p>Since Checkstyle 13.3.0</p>
      <subsection name="Description" id="GoogleNonConstantFieldName_Description">
        <div>
          Checks that non-constant field names conform to the
          <a href=
          "https://google.github.io/styleguide/javaguide.html#s5.2.5-non-constant-field-names">
          Google Java Style Guide</a> for non-constant field naming.
        </div>

        <p>
          This check enforces Google's specific non-constant field naming requirements:
        </p>
        <ul>
        <li>Non-constant field names must start with a lowercase letter and use uppercase letters
          for word boundaries.</li>
        <li>Underscores may be used to separate adjacent numbers (e.g., version
          numbers like <code>guava33_4_5</code>), but NOT between letters and digits.</li>
        </ul>

        <p>
          Static fields are skipped because Checkstyle cannot determine type immutability
          to distinguish constants from non-constants. Fields in interfaces and annotations
          are also skipped because they are implicitly <code>public static final</code> (constants)
        </p>
      </subsection>

      <subsection name="Examples" id="GoogleNonConstantFieldName_Examples">
        <p id="Example1-config">
          Example with violations:
        </p>
        <div class="wrapper"><pre class="prettyprint"><code class="language-xml">
&lt;module name="Checker"&gt;
  &lt;module name="TreeWalker"&gt;
    &lt;module name="GoogleNonConstantFieldName"/&gt;
  &lt;/module&gt;
&lt;/module&gt;
</code></pre></div>
        <p id="Example1-code">Code Example:</p>
        <div class="wrapper"><pre class="prettyprint"><code class="language-java">
class Example1 {

  static final int STATIC_FINAL = 0;
  private static final int PRIVATE_STATIC_FINAL = 1;

  static int staticField;
  private static int privateStatic;

  interface ExampleInterface {
    int CONSTANT = 1;
    String Invalid_Name = "test";
  }

  @interface ExampleAnnotation {
    int DEFAULT = 0;
    String Bad_Name = "test";
  }

  final int bad = 0;
  final int Bad = 0;
  // violation above, 'Non-constant field name 'Bad' must start lowercase'

  public final int httpClient = 0;
  public final int mValue = 0;
  // violation above, 'avoid single lowercase letter followed by uppercase'

  private final int foo = 0;
  private final int f = 0; // violation, 'be at least 2 chars'

  protected final int fooBar1_1 = 0;
  protected final int foo_bar = 0;
  // violation above, 'underscores allowed only between adjacent digits'

  int loginFails;
  int fA; // violation, 'avoid single lowercase letter followed by uppercase'

  int fooBar2;
  int foo$bar; // violation, 'contain only letters, digits or underscores'

  int version1_2_3;
  int gradle_9_5_1; // violation, 'underscores allowed only between adjacent digits'

  int count99;
  int _foo; // violation, 'underscores allowed only between adjacent digits'
}
</code></pre></div>
      </subsection>

      <subsection name="Example of Usage" id="GoogleNonConstantFieldName_Example_of_Usage">
        <ul>
        </ul>
      </subsection>

      <subsection name="Violation Messages" id="GoogleNonConstantFieldName_Violation_Messages">
        <ul>
          <li>
            <a href="https://github.com/search?q=path%3Asrc%2Fmain%2Fresources%2Fcom%2Fpuppycrawl%2Ftools%2Fcheckstyle%2Fchecks%2Fnaming%20path%3A**%2Fmessages*.properties+repo%3Acheckstyle%2Fcheckstyle+%22google.non.constant.field.name.format%22">
              google.non.constant.field.name.format
            </a>
          </li>
        </ul>
        <p>
          All messages can be customized if the default message doesn't suit you.
          Please <a href="../../config.html#Custom_messages">see the documentation</a>
          to learn how to.
        </p>
      </subsection>

      <subsection name="Package" id="GoogleNonConstantFieldName_Package">
        <p> com.puppycrawl.tools.checkstyle.checks.naming </p>
      </subsection>

      <subsection name="Parent Module" id="GoogleNonConstantFieldName_Parent_Module">
        <p>
          <a href="../../config.html#TreeWalker">TreeWalker</a>
        </p>
      </subsection>
    </section>
  </body>
</document>
