<?xml version="1.0" encoding="UTF-8"?>
<document xmlns="http://maven.apache.org/XDOC/2.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/XDOC/2.0 https://maven.apache.org/xsd/xdoc-2.0.xsd">
  <head>
    <title>UnusedTryResourceShouldBeUnnamed</title>
  </head>
  <body>
    <section name="UnusedTryResourceShouldBeUnnamed">
      <p>Since Checkstyle 13.5.0</p>
      <subsection name="Description" id="UnusedTryResourceShouldBeUnnamed_Description">
        <div>
          Ensures that try-with-resources resource variables that are not used
          are declared as an unnamed variable.
        </div>

        <p>
          Rationale:
        </p>
        <ul>
        <li>
          Improves code readability by clearly indicating which resources are unused.
        </li>
        <li>
          Follows Java conventions for denoting unused variables with an underscore
          (<code>_</code>).
        </li>
        </ul>

        <p>
          Only declared resources inside the try-with-resources parentheses are checked
          (i.e. <code>var a = lock()</code> or <code>AutoCloseable a = lock()</code>).
          Resources that are referenced but not declared inside the try
          (e.g. <code>try (releaser) { }</code>) are never flagged, because those resources
          cannot be replaced with <code>_</code>.
        </p>

        <p>
          See the <a href="https://docs.oracle.com/en/java/javase/21/docs/specs/unnamed-jls.html">
          Java Language Specification</a> for more information about unnamed variables.
        </p>

        <p>
          <b>Attention</b>: This check should be activated only on source code
          that is compiled by jdk21 or higher;
          unnamed variables came out as a preview feature in Java 21 and
          became a standard part of the language in Java 22.
        </p>
      </subsection>
      <subsection name="Examples" id="UnusedTryResourceShouldBeUnnamed_Examples">
        <p id="Example1-config">
          To configure the check:
        </p>
        <div class="wrapper"><pre class="prettyprint"><code class="language-xml">
&lt;module name="Checker"&gt;
  &lt;module name="TreeWalker"&gt;
    &lt;module name="UnusedTryResourceShouldBeUnnamed"&gt;
    &lt;/module&gt;
  &lt;/module&gt;
&lt;/module&gt;
</code></pre></div>
        <p id="Example1-code">
          Example:
        </p>
        <div class="wrapper"><pre class="prettyprint"><code class="language-java">
public class Example1 {

  static AutoCloseable lock() {
    return () -&gt; {};
  }

  void example() throws Exception {
    // violation below 'Unused try resource 'a' should be unnamed'
    try (var a = lock()) {
      System.out.println("locked");
    }

    // violation below 'Unused try resource 'res' should be unnamed'
    try (AutoCloseable res = lock()) {
      System.out.println("locked");
    }

    // ok, resource 'b' is used inside the block
    try (var b = lock()) {
      System.out.println(b);
    }

    // ok, already declared as unnamed variable
    try (var _ = lock()) {
      System.out.println("locked");
    }
  }
}
</code></pre></div>
      </subsection>
      <subsection name="Example of Usage" id="UnusedTryResourceShouldBeUnnamed_Example_of_Usage">
        <ul>
          <li>
            <a href="https://github.com/search?q=path%3Aconfig%20path%3A**%2Fcheckstyle-checks.xml+repo%3Acheckstyle%2Fcheckstyle+UnusedTryResourceShouldBeUnnamed">
                Checkstyle Style</a>
          </li>
        </ul>
      </subsection>
      <subsection name="Violation Messages"
            id="UnusedTryResourceShouldBeUnnamed_Violation_Messages">
        <ul>
          <li>
            <a href="https://github.com/search?q=path%3Asrc%2Fmain%2Fresources%2Fcom%2Fpuppycrawl%2Ftools%2Fcheckstyle%2Fchecks%2Fcoding+path%3A**%2Fmessages*.properties+repo%3Acheckstyle%2Fcheckstyle+%22unused.try.resource%22">
              unused.try.resource
            </a>
          </li>
        </ul>
        <p>
          All messages can be customized if the default message doesn't suit you.
          Please <a href="../../config.html#Custom_messages">see the documentation</a>
          to learn how to.
        </p>
      </subsection>
      <subsection name="Fully Qualified Name"
               id="UnusedTryResourceShouldBeUnnamed_Fully_Qualified_Name">
        <p>com.puppycrawl.tools.checkstyle.checks.coding.UnusedTryResourceShouldBeUnnamedCheck</p>
        <p>
          Use this fully qualified class name in configuration when an exact class reference is
          required.
        </p>
      </subsection>
      <subsection name="Parent Module" id="UnusedTryResourceShouldBeUnnamed_Parent_Module">
        <p>
          <a href="../../config.html#TreeWalker">TreeWalker</a>
        </p>
      </subsection>
    </section>
  </body>
</document>
