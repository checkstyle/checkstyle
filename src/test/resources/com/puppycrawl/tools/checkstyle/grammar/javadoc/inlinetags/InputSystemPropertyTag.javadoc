* Returns the value of the {@systemProperty java.io.tmpdir} system property,
* which specifies the default temporary file directory.
* Returns the value of the {@systemProperty java} system property,
* which specifies the default temporary file directory.