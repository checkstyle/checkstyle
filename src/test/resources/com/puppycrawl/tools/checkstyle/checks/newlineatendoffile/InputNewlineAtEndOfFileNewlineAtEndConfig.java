/*
NewlineAtEndOfFile
lineSeparator = LF
fileExtensions = (default)""


*/

package com.puppycrawl.tools.checkstyle.checks.newlineatendoffile;

// Config-only sidecar for testFileWithEmptyLineOnly. The actual target lives
// in InputNewlineAtEndOfFileNewlineAtEnd.txt in the same directory and must
// stay a single LF byte so the "file with only an empty line" case is
// preserved for the check under an LF lineSeparator.
public class InputNewlineAtEndOfFileNewlineAtEndConfig {
}
