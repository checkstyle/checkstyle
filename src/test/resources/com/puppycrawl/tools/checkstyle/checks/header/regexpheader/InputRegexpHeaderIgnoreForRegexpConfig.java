/*
RegexpHeader
headerFile = (file)InputRegexpHeader1.header
charset = (default)null
fileExtensions = (default)""
header = (default)null
multiLines = (default)


*/

package com.puppycrawl.tools.checkstyle.checks.header.regexpheader;

// Config-only sidecar for testRegexpHeaderIgnore. The actual target lives
// in InputRegexpHeaderIgnore.java in the same directory and must stay
// untouched: an inline config header on the target would be part of what
// RegexpHeaderCheck compares against the configured header patterns.
public class InputRegexpHeaderIgnoreForRegexpConfig {
}
