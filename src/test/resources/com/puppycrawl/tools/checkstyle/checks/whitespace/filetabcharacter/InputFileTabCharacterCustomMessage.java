/*
FileTabCharacter
eachLine = (default)false
fileExtensions = (default)
message.file.containsTab = File contains tab characters (this is the first instance) :)


*/

package com.puppycrawl.tools.checkstyle.checks.whitespace.filetabcharacter;

public class InputFileTabCharacterCustomMessage {
    // Long line ----------------------------------------------------------------
    // Contains a tab ->	<- // violation 'contains tab characters (this is the first instance)'
    // Contains trailing whitespace ->

    // Contains a tab ->	<-
}
