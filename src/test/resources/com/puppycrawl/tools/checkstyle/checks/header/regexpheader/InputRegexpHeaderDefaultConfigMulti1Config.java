/*
RegexpHeader
headerFile = (file)InputRegexpHeader2.header
charset = (default)null
fileExtensions = (default)""
header = (default)null
multiLines = 3, 6


*/

package com.puppycrawl.tools.checkstyle.checks.header.regexpheader;

// Config-only sidecar for testRegexpHeaderMulti1. The actual target lives
// in InputRegexpHeaderDefaultConfig.java in the same directory and must
// stay untouched: an inline config header on the target would be part of
// what RegexpHeaderCheck compares against the configured header patterns.
public class InputRegexpHeaderDefaultConfigMulti1Config {
}
