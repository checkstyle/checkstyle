/*
Header
headerFile = (file)InputHeaderjava.blank-lines2.header
charset = (default)null
fileExtensions = (default)""
header = (default)null
ignoreLines = (default)


*/

package com.puppycrawl.tools.checkstyle.checks.header.header;

// Config-only sidecar for testHeaderIsValidWithBlankLinesBlockStyle. The
// actual target lives in InputHeaderBlankLines2.java in the same directory
// and must stay untouched: an inline config header on the target would be
// part of what HeaderCheck compares against the configured header.
public class InputHeaderBlankLines2Config {
}
