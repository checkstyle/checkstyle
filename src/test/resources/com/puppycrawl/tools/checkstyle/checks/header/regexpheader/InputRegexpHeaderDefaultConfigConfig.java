/*
com.puppycrawl.tools.checkstyle.checks.header.MultiFileRegexpHeaderCheck
headerFiles = (default)
fileExtensions = (default)""


*/

package com.puppycrawl.tools.checkstyle.checks.header.regexpheader;

// Config-only sidecar for testDefaultConfiguration. The actual target lives in
// InputRegexpHeaderDefaultConfig.java in the same directory and must stay
// untouched: an inline config header on the target would collide with what
// MultiFileRegexpHeaderCheck reads as the file's actual header.
public class InputRegexpHeaderDefaultConfigConfig {
}
