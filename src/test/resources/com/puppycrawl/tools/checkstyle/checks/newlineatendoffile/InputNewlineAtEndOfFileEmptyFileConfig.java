/*
NewlineAtEndOfFile
lineSeparator = (default)LF_CR_CRLF
fileExtensions = (default)""


*/

package com.puppycrawl.tools.checkstyle.checks.newlineatendoffile;

// Config-only sidecar for testEmptyFileFile. The actual target lives in
// InputNewlineAtEndOfFileEmptyFile.txt in the same directory and must stay
// empty (zero bytes) so the check's empty-file handling is what is being tested.
public class InputNewlineAtEndOfFileEmptyFileConfig {
}
