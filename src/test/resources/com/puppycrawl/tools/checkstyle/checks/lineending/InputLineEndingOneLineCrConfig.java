/*
LineEnding
lineEnding = (default)LF
fileExtensions = (default)""


*/

package com.puppycrawl.tools.checkstyle.checks.lineending;

// Config-only sidecar for testInputLineEndingOneLineCr. The actual target
// lives in InputLineEndingOneLineCr.txt and stays a single-line file
// terminated by CR so the check's one-line handling is what is being tested.
public class InputLineEndingOneLineCrConfig {
}
