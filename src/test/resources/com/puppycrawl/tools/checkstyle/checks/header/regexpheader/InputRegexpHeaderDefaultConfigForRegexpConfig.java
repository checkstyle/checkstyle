/*
RegexpHeader
headerFile = (default)null
charset = (default)null
fileExtensions = (default)""
header = (default)null
multiLines = (default)


*/

package com.puppycrawl.tools.checkstyle.checks.header.regexpheader;

// Config-only sidecar for testDefaultConfiguration. The actual target lives
// in InputRegexpHeaderDefaultConfig.java in the same directory and must stay
// untouched: an inline config header on the target would be part of what
// RegexpHeaderCheck compares against the configured header.
public class InputRegexpHeaderDefaultConfigForRegexpConfig {
}
