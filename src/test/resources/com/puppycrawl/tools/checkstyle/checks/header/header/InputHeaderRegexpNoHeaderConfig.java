/*
Header
headerFile = (default)null
charset = (default)null
fileExtensions = (default)""
header = (default)null
ignoreLines = (default)


*/

package com.puppycrawl.tools.checkstyle.checks.header.header;

// Config-only sidecar for testNoHeader. The actual target lives in
// InputHeaderRegexp.java in the same directory and must stay untouched:
// an inline config header on the target would be part of what
// HeaderCheck compares against the configured header.
public class InputHeaderRegexpNoHeaderConfig {
}
