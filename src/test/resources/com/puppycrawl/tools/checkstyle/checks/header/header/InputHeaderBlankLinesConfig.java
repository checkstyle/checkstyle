/*
Header
headerFile = (file)InputHeaderjava.blank-lines.header
charset = (default)null
fileExtensions = (default)""
header = (default)null
ignoreLines = (default)


*/

package com.puppycrawl.tools.checkstyle.checks.header.header;

// Config-only sidecar for testHeaderIsValidWithBlankLines. The actual
// target lives in InputHeaderBlankLines.java in the same directory and
// must stay untouched: an inline config header on the target would be
// part of what HeaderCheck compares against the configured header.
public class InputHeaderBlankLinesConfig {
}
