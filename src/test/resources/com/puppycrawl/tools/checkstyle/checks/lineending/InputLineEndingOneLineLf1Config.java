/*
LineEnding
lineEnding = crlf
fileExtensions = (default)""


*/

package com.puppycrawl.tools.checkstyle.checks.lineending;

// Config-only sidecar for testInputLineEndingOneLineLf1. The actual target
// lives in InputLineEndingOneLineLf.txt and must stay a single-line file
// terminated by LF so the check's one-line handling is what is being tested.
public class InputLineEndingOneLineLf1Config {
}
